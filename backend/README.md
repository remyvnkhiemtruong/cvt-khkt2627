# CVT KHKT Backend

Vercel Functions overlay for the React/Vite app. Current phase supports the manual workflow: student submit -> researcher feedback -> teacher feedback. AI contract is present but disabled (AI_FEEDBACK_ENABLED=false).

Health: /api/health
